# 表示対象の GitHub ログイン名（固定運用）。
GITHUB_USERNAME=your-login

# GitHub PAT（classic の read:user を推奨）。サーバー側だけで使い、ブラウザへは渡さない。
# 本番は `wrangler secret put GITHUB_TOKEN` で設定する。
GITHUB_TOKEN=github_pat_replace_me

# 非公開リポジトリのコントリビューションを合計に含めるか。
INCLUDE_PRIVATE=false
